/*
**  InitGetI()
**		All this does is define some global variables for the keys
**		used in GetInput(3) and assign some default values.
*/

#include	<curses.h>
#include	"GetInput.h"

void
InitGetI()
{
/*
	char  *tName, *lName, *getenv() ;

   	lName = longname() ;
   	tName = getenv("TERM") ;

   	if (strcmp(tName, "5425")  ==  0)
      		Load5425 () ;
   	else
		if (strcmp (tName, "sun") == 0)
			LoadSun ();
		else
      			LoadAnsi() ;
*/
}
